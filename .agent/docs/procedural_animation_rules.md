# 🦴 Правила и Шаблоны Процедурной Анимации Врагов (IK)

Этот документ описывает стандарты и подводные камни создания процедурных анимаций на основе инверсной кинематики (IK) для врагов в NewTower. При создании новых сложных противников (например, Орк, Гоблин, Тролль) **всегда** следуйте этим правилам, чтобы избежать визуальных артефактов и "крабьей походки".

---

## 1. Проблема Перспективы (DIR3)

В игре используется 3 направления (DIR3): `SIDE` (сбоку), `UP` (сзади) и `DOWN` (спереди). Главная ошибка при использовании 2D IK — прямая проекция расчетов вида сбоку на виды спереди/сзади.

### ❌ Ошибка "Крабья походка" (Раскоряченные ноги)

Если во фронтальном виде задать смещение ног по оси X (влево/вправо) так же, как в виде сбоку, колени выгнутся наружу, и персонаж будет двигаться боком, как краб.

### ✅ Решение: Раздельная логика IK (2D IK vs Проекционный IK)

При написании метода `applyProceduralIK(p: SkeletonPose)` всегда жестко разделяйте логику:

```typescript
if (p.facing === 'SIDE') {
    // 1. ИСТИННЫЙ 2D IK (Вид сбоку)
    // - Полная свобода по оси X (шаг вперед-назад)
    // - Колени сгибаются математически точно (solve2BoneIK)
    // - Руки размахивают по оси X
} else {
    // 2. ПРОЕКЦИОННЫЙ ПСЕВДО-3D IK (Виды спереди 'DOWN' и сзади 'UP')
    // - Запрет на широкие колебания по оси X! Ступни и кисти двигаются по оси Y (имитация глубины).
    // - Шаг (strideDepth) компенсируется оптическим "укорачиванием" длины костей, а не их изгибом в стороны.
    // - Колени и локти лишь СЛЕГКА сдвигаются вбок для имитации перспективы.
}
```

---

## 2. Анатомия и Пропорции (Защита от NaN и разрывов)

Если расстояние от начальной точки (таз) до конечной (ступня) превышает сумму длин двух костей (бедро + икра), IK-солвер перестает нормально работать, возвращает `NaN` или рисует кости вытянутой прямой линией, игнорируя изгиб колена.

**Правила геометрии тела:**

1. **Задел на изгиб:** Общее расстояние от пола (`feetY`) до таза ОШИБКА должно быть строго МЕНЬШЕ суммы длин костей ног (`THIGH_LEN + CALF_LEN`).
   * *Пример:* Если ноги 6.5 + 6.5 = 13, таз должен находиться на высоте максимум 10-11 от пола, чтобы колени физически могли согнуться.
2. **Центр тяжести:** Таз (`pelvis`) лучше спускать ниже, а не задирать вверх. Это дает простор для IK ног.
3. **Позвоночник и Шея:** Шея не должна "висеть" слишком далеко от таза. Сближайте их, чтобы плечи и голова не парили отдельно от ног.

---

## 3. Чек-лист создания Procedural Renderer-а

При создании нового `Base[Monster]Renderer.ts` (например, `BaseOrcRenderer`):

* [ ] Создайте объект `Pose` (напр., `OrcPose`), содержащий `joints` (суставы) и `anchors` (якоря крепления оружия/предметов).
* [ ] Разделите логику вышагивания в `SIDE` и `UP/DOWN`.
* [ ] В проекции `UP/DOWN` используйте `Math.sin(cycle)` для изменения координаты `Y` ступней (`strideDepth`), имитируя шаг в глубину экрана.
* [ ] Заблокируйте сильный изгиб коленей в `UP/DOWN`: просто сокращайте расстояние (компрессируйте кости).
* [ ] Защитите `solve2BoneIK` от деления на `0` и переразгибов (`clamp`).
* [ ] Отрисовывайте ступни с учетом ракурса (в `DOWN` они должны "смотреть" чуть в стороны и вниз, в `UP` — наружу и вверх).
* [ ] Для контраста с игровой дорогой **всегда** добавляйте нижний слой-обводку (Shadow Pass) для костей. Делайте её толще на 2px и темного контрастного цвета (например, темно-коричневого или иссиня-черного).

```typescript
// Пример: Отрисовка ступней с учетом перспективы
let dirX = (facing === 'UP') ? (isLeft ? -1.5 : 1.5) : (isLeft ? -2 : 2);
let dirY = (facing === 'UP') ? -1 : 2;
// для SIDE стопа всегда смотрит вперед
```
