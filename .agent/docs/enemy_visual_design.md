# 👹 Enemy Visual Design Document

## 1. Общая Концепция

### Стиль

**Fantasy-Realistic Canvas Art** — детализированные процедурные враги с реалистичными пропорциями и AAA-качеством рендера.

### Принципы

1. **Читаемость** — силуэт врага понятен с первого взгляда
2. **Детализация** — каждый враг должен иметь минимум 10-14 уникальных визуальных элементов
3. **Анимация** — плавные циклы ходьбы, дыхание, процедурные движения
4. **Консистентность** — все виды (Side/Front/Back) имеют одинаковый уровень детализации
5. **Производительность** — никаких `ctx.filter`, только градиенты и альфа-блендинг

---

## 2. Обязательные Элементы

### Для КАЖДОГО рендера врага (минимум)

| Элемент | Описание | Пример реализации |
|---------|----------|-------------------|
| **Основное тело** | Мускулатура, пропорции | Мышцы плеч, торс, талия |
| **Конечности** | Руки/щупальца/крылья | Анимация ходьбы, размах |
| **Ноги** | Правильное подключение к талии | 2 ноги минимум, connected at waist |
| **Голова** | Пропорциональная размеру тела | Шлем, череп, морда |
| **Текстуры** | Шкура/металл/кожа | Градиенты, складки кожи |
| **Детали** | Шрамы, оружие, броня | Embedded debris, stitches |
| **Свечение** | Глаза, раны, магия | Radial gradients, shadowBlur |
| **Тени** | Объем и глубина | Drop shadows, dark edges |
| **Дыхание** | Живая анимация | `Math.sin(t * 1.5)` pulse |
| **Damage FX** | При низком HP | Кровь, трещины, gore |

---

## 3. Архетипы Врагов

### 💀 SKELETON — Скелет

**Концепция:** Классический нежить-воин в потертой броне

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Щит + меч, ребра, череп в шлеме, плащ |
| Front | Грудная клетка видна, оружие параллельно камере |
| Back | Позвоночник, спина щита, затылок шлема |

**Анимация:**

- Медленное покачивание при ходьбе
- Оружие слегка движется
- Глаза светятся красным (`shadowBlur`)

---

### 🐕 HELLHOUND — Адская Гончая

**Концепция:** Крупный демонический пес с тлеющей шерстью

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | 4 ноги, хвост с пламенем, открытая пасть |
| Front | Широкая морда, клыки, горящие глаза |
| Back | Пылающий хребет, хвост вверх |

**Анимация:**

- Галопирующая походка (4-point cycle)
- Пламя мерцает (`Math.sin(t * 4)`)
- Слюна капает при атаке

**FX:**

- Огненный trail (но НЕ статичный!)
- Heat distortion вокруг тела

---

### 👹 ORC — Орк Воевода

**Концепция:** Массивный бронированный воин с двуручным топором

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Тяжелая броня, топор на плече, бивни |
| Front | Широкие плечи, грудь защищена, рога на шлеме |
| Back | Шипастый наплечник, цепи |

**Анимация:**

- Тяжелый шаг с отскоком
- Броня звенит (визуально — subtle shake)
- Топор качается

---

### 👻 WRAITH — Призрак Пустоты

**Концепция:** Эфирная сущность с мерцающим телом

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Рваный плащ, туманное тело, капюшон |
| Front | Пустые глазницы, руки-когти |
| Back | Плащ развевается, призрачный шлейф |

**Анимация:**

- Парящее движение (sin wave Y offset)
- Fade in/out (`globalAlpha` pulse)
- Частицы тумана вокруг

**FX:**

- Полупрозрачность (`globalAlpha: 0.7-0.9`)
- Glow вокруг тела

---

### 👑 SKELETON_COMMANDER — Командир Скелетов

**Концепция:** Элитный скелет с большим мечом и роскошным плащом

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Массивный меч, плащ-накидка, золотая корона |
| Front | Орнаментированная броня, меч в агрессивной стойке |
| Back | Плащ с геральдикой, меч за спиной |

**Детали:**

- Плащ имеет физику (3-4 control points, bezier curves)
- Золотые акценты (`#ffd700`)
- Пульсирующая аура силы

---

### 🕷️ SPIDER — Ядовитый Паук

**Концепция:** 8-ногий паук с капающим ядом

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | 4 ноги видны, брюшко, челюсти |
| Front | Все 8 глаз, клыки, раздутое брюшко |
| Back | Паутинные железы, узор на спине |

**FX:**

- Яд капает с клыков
- Зеленое свечение (`#76ff03`)

---

### 🛡️ TROLL — Латник

**Концепция:** Огромный снежный тролль с дубиной

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Массивная дубина, широкая спина |
| Front | Тройной подбородок, широкие плечи |
| Back | Кольчуга, шрамы |

**Цвет:** Холодный серо-синий (`#cfd8dc`)

---

### 👺 GOBLIN — Гоблин-Мародер

**Концепция:** Мелкий зеленый грабитель с мешком

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Мешок за спиной, кривой нос |
| Front | Большие уши, золотые зубы |
| Back | Выпирающий мешок, кость в поясе |

**Анимация:**

- Суетливый бег
- Мешок подпрыгивает

---

### 🐀 RAT — Алхимическая Крыса

**Концепция:** Крыса с бомбой на спине

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Горящий фитиль, 4 лапы |
| Front | Усы, красные глаза, баллон на спине |
| Back | Бомба с символом черепа |

**FX:**

- Фитиль искрит
- Взрыв при смерти (EffectSystem)

---

### 🌋 MAGMA — Король Магмы

**Концепция:** Гуманоид из лавы с остывающей корой

| Вид | Ключевые элементы |
|-----|-------------------|
| Side | Трещины со светом, корона из магмы |
| Front | Пылающие глаза, лавовое сердце |
| Back | Остывшая спина, трещины |

**Механика:**

- При уроне сбрасывает куски брони
- Переход в MAGMA_STATUE форму

**FX:**

- Оранжевое свечение из трещин
- Heat distortion

---

### 🧟 FLESH — Мясной Колосс

**Концепция:** Омерзительная груда плоти с щупальцами

> **ВАЖНО:** Самый детализированный враг в игре (14+ элементов)

| Вид | Ключевые элементы (все 14) |
|-----|----------------------------|
| **Side** | 1. Back tentacles<br>2. Far leg<br>3. Muscular upper body<br>4. Bloated belly<br>5. Fat rolls<br>6. Embedded debris<br>7. Organs (visible)<br>8. Stitches<br>9. Near leg<br>10. Front tentacles<br>11. Embedded faces<br>12. Small grotesque head<br>13. Dripping gore<br>14. Damage effects |
| **Front** | Все те же элементы, адаптированные для вида спереди |
| **Back** | Спинной хребет, шрамы, больше щупалец |

**Анимация:**

- Медленное покачивание всего тела
- Щупальца извиваются независимо
- Живот пульсирует (что-то движется внутри)
- Кровь капает постоянно

**Пропорции:**

```
Shoulders: широкие, мускулистые
Belly: огромный, свисающий
Legs: толстые, подключены к талии
Head: МАЛЕНЬКИЙ (0.6x scale)
```

---

## 4. Технические Стандарты

### Обязательные методы в каждом UnitRenderer

```typescript
class ExampleUnitRenderer extends UnitRenderer {
    // Основной рендер с определением направления
    drawBody(ctx, enemy, scale, rotation): void
    
    // Виды (все 3 обязательны)
    private drawSide(ctx, s, cycle, isMoving, t, hp)
    private drawFront(ctx, s, cycle, isMoving, t, hp)
    private drawBack(ctx, s, cycle, isMoving, t, hp)
    
    // Вспомогательные методы (по необходимости)
    private drawLegSide(ctx, x, y, cycle, rotation, s, isFar)
    private drawHead(ctx, s, t)
    private drawWeapon(ctx, s, t)
    
    // Эффекты (опционально)
    drawEmissive(ctx, enemy, scale, rotation): void
}
```

---

### Анимационные переменные (стандарт)

```typescript
const sway = isMoving ? Math.sin(cycle) * 0.06 : Math.sin(t * 0.5) * 0.02;
const bounce = isMoving ? Math.abs(Math.sin(cycle * 0.5)) * 3 * s : 0;
const breathe = Math.sin(t * 1.5) * 0.08;
const pulse = 1 + breathe;
```

---

### Определение направления

```typescript
let facing: 'DOWN' | 'UP' | 'SIDE' = 'SIDE';
const r = rotation;
if (r > -2.35 && r < -0.78) facing = 'UP';    // Back view
else if (r > 0.78 && r < 2.35) facing = 'DOWN'; // Front view
else facing = 'SIDE';                            // Side view
```

---

## 5. Цветовые Палитры

### Базовые цвета (константы класса)

```typescript
static readonly FLESH_PALE = '#d4a59a';
static readonly FLESH_LIGHT = '#c89f91';
static readonly FLESH_MID = '#b88b7d';
static readonly FLESH_DARK = '#8b6f64';
static readonly BLOOD_BRIGHT = '#e53935';
static readonly BLOOD_DARK = '#b71c1c';
```

### По архетипу

| Архетип | Основной | Акцент | Свечение |
|---------|----------|--------|----------|
| SKELETON | `#e0e0e0` | `#757575` | `#f44336` (глаза) |
| HELLHOUND | `#212121` | `#ff5722` | `#ff9800` (пламя) |
| ORC | `#558b2f` | `#33691e` | — |
| WRAITH | `#1a0b2e` | `#4a148c` | `#9c27b0` (аура) |
| SPIDER | `#689f38` | `#76ff03` | `#76ff03` (яд) |
| MAGMA | `#ff3d00` | `#ffab00` | `#ffea00` (лава) |
| FLESH | `#d4a59a` | `#8b6f64` | `#e53935` (кровь) |

---

## 6. Запрещенные Практики

### ❌ НЕ делать

1. **Статичные эффекты** — всё должно двигаться или пульсировать

   ```typescript
   // ПЛОХО:
   this.drawSlimeTrail(ctx, x, y); // Static puddle
   
   // ХОРОШО:
   this.drawDrippingGore(ctx, s, t, isMoving); // Animated drips
   ```

2. **ctx.filter для hit flash** — вызывает зависание

   ```typescript
   // ПЛОХО:
   if (hitFlash) ctx.filter = 'brightness(2.0)';
   
   // ХОРОШО:
   if (hitFlash) ctx.globalAlpha = 0.5 + Math.sin(t * 30) * 0.5;
   ```

3. **Несвязанные ноги** — всегда подключать к талии

   ```typescript
   // ПЛОХО:
   this.drawLeg(ctx, 0, 0); // Floating leg
   
   // ХОРОШО:
   this.drawLeg(ctx, -3 * s, 6 * s); // Connected to waist
   ```

4. **Разная детализация по видам**

   ```typescript
   // ПЛОХО:
   drawFront() { /* 3 elements */ }
   drawSide() { /* 14 elements */ }
   
   // ХОРОШО:
   // All views have same element count
   ```

5. **Непропорциональные головы**

   ```typescript
   // ПЛОХО:
   this.drawHead(ctx, s * 2.0); // Too big
   
   // ХОРОШО:
   this.drawHead(ctx, s * 0.6); // Small, grotesque
   ```

---

## 7. Производительность

### Оптимизации

1. **Использовать `ctx.save/restore` локально**

   ```typescript
   ctx.save();
   ctx.translate(x, y);
   // ... drawing
   ctx.restore(); // Clean up transformations
   ```

2. **Группировать однотипные операции**

   ```typescript
   // Draw all dark elements together
   ctx.fillStyle = DARK_COLOR;
   ctx.fill(); // Multiple paths with same color
   ```

3. **Избегать частых `beginPath()`**

   ```typescript
   // ПЛОХО: 10 beginPath calls
   // ХОРОШО: 1 beginPath для всей фигуры
   ```

4. **Кэшировать вычисления**

   ```typescript
   const bellyPulse = 1 + Math.sin(t * 1.5) * 0.08;
   // Use bellyPulse multiple times
   ```

---

## 8. Чеклист качества

Перед финализацией врага проверить:

- [ ] Все 3 вида реализованы
- [ ] Минимум 10 визуальных элементов на вид
- [ ] Ноги подключены к талии (не висят в воздухе)
- [ ] Голова пропорциональна телу
- [ ] Есть анимация дыхания/покачивания
- [ ] Damage effects при hp < 50%
- [ ] Цвета соответствуют архетипу
- [ ] Нет статичных эффектов
- [ ] Нет использования `ctx.filter`
- [ ] Scale корректен (помещается на дороге)
- [ ] Hit flash работает через `globalAlpha`

---

## 9. Примеры Референсов

### Лучшие реализации в проекте

1. **FleshUnitRenderer.ts** — эталон детализации (14 элементов)
2. **SkeletonCommanderUnitRenderer.ts** — отличная физика плаща
3. **HellhoundUnitRenderer.ts** — анимация галопа
4. **MagmaUnitRenderer.ts** — интересная механика смены форм

### Что изучить

- `drawMainBodySide` в FleshUnitRenderer — мускулатура
- `drawCape` в SkeletonCommanderUnitRenderer — cloth physics
- `drawLegSide` в любом рендере — правильная анимация ходьбы
