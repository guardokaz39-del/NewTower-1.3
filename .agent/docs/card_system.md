# 🃏 Card System & Mechanics

## 1. Общая Концепция

Система карт — это **основной способ улучшения башен**. Игрок получает карты за убийство врагов и волны, затем перетаскивает их на башни или в кузницу для объединения.

---

## 2. Типы Карт

### 🔥 FIRE — Огненная карта

**Концепция:** AoE урон, горение, взрывы

| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| ★ | +12 урон, -15% скорость | Splash 45px |
| ★★ | +22 урон, -10% скорость | Splash 70px |
| ★★★ | +25 урон, -10% скорость | Splash 80px, 50% взрывной урон |

**Тип снаряда:** `FIRE`  
**Визуальный эффект:** Огненный шар, trail из искр, взрыв при попадании

---

### ❄️ ICE — Ледяная карта

**Концепция:** Замедление, контроль, эффекты на замедленных врагов

| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| ★ | +1 урон, -10% дальность | Slow 3s (30%) |
| ★★ | +3 урон, -20% дальность | Slow 4.5s (45%), +20% к замедленным |
| ★★★ | +6 урон, -25% дальность | Slow 6s (75%), +40% к замедленным, Chain Ice (80px) |

**Тип снаряда:** `ICE`  
**Визуальный эффект:** Осколок льда, frost trail, ледяная волна при Lv3

---

### 🎯 SNIPER — Снайперская карта

**Концепция:** Дальность, высокий урон, критические удары

| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| ★ | +14 урон, +80 дальность, -70% скорость | 15% крит, **Precise Aim** |
| ★★ | +24 урон, +160 дальность, -55% скорость | 15% крит, **Precise Aim** |
| ★★★ | +46 урон, +240 дальность, -40% скорость | 20% крит, Pierce 2, **Precise Aim** |

> **Precise Aim:** Башня поворачивается медленно (5.0) и требует точного наведения (8.5°).

**Тип снаряда:** `SNIPER`  
**Визуальный эффект:** Лазерный луч, прицеливание (laser sight)

---

### 💥 MULTISHOT — Залповая карта

**Концепция:** Несколько снарядов за выстрел

| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| ★ | 0.60x урон | 2 снаряда, spread 0.30 |
| ★★ | 0.70x урон | 2 снаряда, spread 0.20 (точнее) |
| ★★★ | 0.55x урон | 3 снаряда, spread 0.25 |

**Примечание:** Multiple Multishot карты складываются аддитивно:

- 2 карты Lv1 = 2 снаряда (аддитивность уточняется)
- Базовый стакинг работает через `getMultishotConfig` (нелинейно)

**Тип снаряда:** Наследуется от других карт  
**Визуальный эффект:** Веерный залп

---

### ⚡ MINIGUN — Пулеметная карта

**Концепция:** Скорострельность, перегрев, высокая скорость атаки

| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| Уровень | Модификаторы | Эффекты |
|---------|--------------|---------|
| ★ | x2.65 скорость, 0.55x урон | Spinup (+4 DPS, 5s max), **Fast Turn** |
| ★★ | x2.80 скорость, 0.65x урон | Spinup (+5 DPS, +2.5% crit/s), **Fast Turn** |
| ​​★★★ | x2.75 скорость, 0.80x урон | Spinup (Stepped Damage +6 to +45), **Fast Turn** |

> **Fast Turn:** Башня вертится быстро (15.0) и имеет широкий сектор обстрела (34°).

**Механика:**

- Башня "разгоняется" — стреляет медленно, затем ускоряется до максимума
- Разгон сбрасывается при смене цели
- **Пенальти за стакинг:** каждая дополнительная Minigun карта уменьшает урон на 5-9%

**Тип снаряда:** `MINIGUN`  
**Визуальный эффект:** Вращающиеся стволы, трассеры, heat glow при перегреве

---

## 3. Механика Стакинга Карт

### Общие правила

Когда на башню добавлено **несколько карт**:

#### Разные типы карт

- ✅ Все бонусы суммируются **полностью (100%)**
- Пример: FIRE Lv2 + ICE Lv1 = оба дают 100% своих бонусов

#### Одинаковые типы карт

- 🏆 **Самая сильная карта** = 100% бонусов
- 🔸 Остальные карты дают **уменьшенный бонус** в зависимости от уровня:

| Уровень | Вклад в стак |
|---------|--------------|
| ★★★ | 70% |
| ★★ | 50% |
| ★ | 35% |

**Пример:**

```
Башня с 3 картами FIRE:
- FIRE Lv3: +30 урон (100%) = 30
- FIRE Lv2: +10 урон (50%) = 5
- FIRE Lv1: +5 урон (35%) = 1.75
ИТОГО: +36.75 урон
```

---

### Особые случаи

#### MINIGUN — Пенальти за стакинг

Minigun **НЕ следует** обычным правилам стакинга:

- Первая карта (высший уровень) = базовый damageMultiplier
- Каждая дополнительная карта **уменьшает** урон:
  - Lv1: -5%
  - Lv2: -7%
  - Lv3: -9%

**Пример:**

```
2 карты Minigun Lv3:
- Первая: 0.35x урон
- Вторая: -9% penalty
Итого: 0.26x урон (0.35 - 0.09)

Минимум: 0.10x (не может быть ниже 10%)
```

#### MULTISHOT — Аддитивный стакинг

Multishot карты **складываются** напрямую:

```
Lv3 = 4 снаряда
Lv2 = 3 снаряда
Lv1 = 2 снаряда

Lv3 + Lv1 = 4 + 2 = 6 снарядов
```

---

## 4. Система Эффектов

### Типы эффектов

| Тип | Описание | Применение |
|-----|----------|------------|
| `FIRE` | AoE урон + горение | При попадании |
| `ICE` | Замедление | При попадании |
| `LASER_SIGHT` | Целеуказание | Визуальный |
| `SPLASH` | Эксплозивный урон | При попадании |
| `CHAIN_ICE` | Цепное замедление (Lv3) | На соседей |
| `PIERCE` | Пробитие | Через врагов |
| `SPINUP` | Разгон стволов | Minigun |

---

### Приоритет эффектов при стакинге

Если несколько карт дают **один и тот же эффект**, используется **наибольшее значение**:

```typescript
// Пример: две ICE карты
ICE Lv3: slowPower = 0.7, slowDuration = 7
ICE Lv1: slowPower = 0.3, slowDuration = 3

ИТОГ: slowPower = 0.7, slowDuration = 7 (max)
```

---

## 5. Кузница (Forge System)

**Назначение:** Объединение 2 карт одного типа и уровня в карту **следующего уровня**.

### Правила

```
2x FIRE Lv1 → 1x FIRE Lv2
2x FIRE Lv2 → 1x FIRE Lv3
```

### Ограничения

- Нельзя объединить **Lv3** (это максимум)
- Нельзя объединить **разные типы** карт
- Нельзя объединить **разные уровни**
- Нельзя скрестить **разные эволюционные пути**

### Система Эволюции

При переходе на Lv2 (и далее), кузница предлагает **выбор эволюционного пути** через модальное окно:

- **Path A** и **Path B** — два варианта эволюции с уникальным стилем игры
- Выбранный путь записывается в `card.evolutionPath`
- Карты с **разными** evolutionPath **нельзя** ковать вместе

### UI Кузницы

- 2 слота с классами `.forge-slot.slot-empty`
- Пустые слоты показывают "+" и "Карта" через CSS `::before`/`::after`
- При заполнении — `slot-empty` удаляется, карта рендерится с `scale(0.8)`
- Кнопка ковки: "⚒️ Ковка" (disabled) → "⚒️ {cost}💰" (enabled)

---

## 6. Получение Карт

### Источники

1. **Убийство врагов** — 10% шанс дропа случайной карты
2. **Завершение волны** — 1-2 гарантированные карты
3. **Бонусы** — дополнительные карты за перформанс

### Рандомизация

Карты выдаются **случайно**, но с весами:

```typescript
FIRE: 20%
ICE: 20%
SNIPER: 20%
MULTISHOT: 20%
MINIGUN: 20%
```

---

## 7. Drag & Drop Система

### Интерфейс

- **Рука карт** — внизу экрана (overlap-fan при >5 картах)
- **Перетаскивание** — drag карту на башню или в кузницу
- **Подсветка** — valid drop zones подсвечиваются

### Визуальный фидбек

#### Drop Zones (Canvas)

```typescript
// Valid drop
ctx.strokeStyle = '#4caf50'; // Green
ctx.lineWidth = 3;

// Invalid drop
ctx.strokeStyle = '#f44336'; // Red
ctx.lineWidth = 2;
```

#### Drag States (CSS)

| Класс | Эффект |
|-------|--------|
| `.card.dragging-placeholder` | Остаток в руке: `opacity: 0.25`, `grayscale(0.6)`, `scale(0.95)` |
| `#drag-ghost` | Ghost: `scale(1.08)`, `brightness(1.1)`, `drop-shadow` |

---

## 8. Технические Детали

### Структура ICard

```typescript
interface ICard {
    type: CardType;         // Тип карты (FIRE, ICE, etc.)
    level: number;          // 1, 2, или 3
    instanceId: string;     // Уникальный ID экземпляра
}
```

### Структура CardType

```typescript
interface CardType {
    id: string;             // 'fire', 'ice', etc.
    name: string;           // "Огненная карта"
    color: string;          // '#f44336'
    symbol: string;         // '🔥'
}
```

### Расчет статов башни

```typescript
// В Tower.ts
getStats() {
    const merged = mergeCardsWithStacking(this.cards);
    
    // Базовые статы
    let stats = { damage: BASE_DAMAGE, range: BASE_RANGE, ... };
    
    // Применение модификаторов
    stats.damage += merged.modifiers.damage;
    stats.range *= merged.modifiers.rangeMultiplier;
    stats.attackSpeed *= merged.modifiers.attackSpeedMultiplier;
    
    return stats;
}
```

---

## 9. Визуальная Обратная Связь

### На башне

```typescript
// Glow по типу карты
if (hasFireCard) {
    ctx.shadowColor = '#ff9800';
    ctx.shadowBlur = 15;
}

// Масштаб по уровню
scale *= (1.0 + (maxCardLevel * 0.15));
```

### На картах

| Элемент | Реализация |
|---------|-------------|
| Фон | Градиент по цвету карты |
| Рамка | Толщина по уровню (Lv3 = thick + gold) |
| Звезды | 1-3 звездочки сверху |
| Символ | Эмодзи типа карты (🔥❄️🎯) |
| Анимация | Hover lift + glow pulsation |
| Эволюция | `.evo-badge` (верхний правый угол) |

### В руке (Hand Fan)

| Число карт | Поведение |
|-----------|----------|
| ≤5 | Нормальный `gap: 8px` |
| >5 | Overlap `margin-left: -12px`, hover раздвигает |

### В слотах (Shop/Forge)

- Карта автоматически уменьшена до `scale(0.8)`
- Слот `80×112px` с dashed border и glass background
- Shop slot: gold border, neon glow при выборе
- Forge slot: orange border, "+ Карта" в пустом состоянии

---

## 10. Баланс

### Формулы урона

```typescript
// Fire
totalDamage = baseDamage + cardDamage
splashDamage = totalDamage * explosionPercent

// Ice
damageToSlowed = baseDamage * (1 + damageToSlowedBonus)

// Sniper
critDamage = baseDamage * 2.0 (if crit)

// Minigun
minigunDamage = baseDamage * damageMultiplier
```

---

## 11. Примеры Сборок

### "Огненный ад" (Fire focus)

```
3x FIRE Lv3 = огромный AoE урон
+ 1x MULTISHOT Lv2 = много огненных шаров
```

### "Ледяной контроль" (Ice focus)

```
2x ICE Lv3 = chain slow на всех врагов
+ 1x MULTISHOT Lv1 = больше targets
```

### "Снайпер-убийца" (Sniper focus)

```
1x SNIPER Lv3 = дальность + крит
+ 2x SNIPER Lv1 (35% каждая) = дополнительный урон и pierce
```

### "Пулеметная турель" (Minigun focus)

```
1x MINIGUN Lv3 = максимальная скорострельность
+ 1x FIRE Lv2 = AoE эффект на быстрых выстрелах
```

---

## 12. Важные Ограничения

### ❌ НЕ работает

- Снайпер + Minigun на **одной башне** (конфликт механик)
- Больше 10 карт в руке (лимит UI)
- Объединение разных типов в кузнице

### ✅ Работает

- Любые комбинации разных типов
- Стакинг до 10 карт одного типа на башню
- Multishot работает со **всеми** типами снарядов

---

## 13. Код-Референсы

### Ключевые файлы

| Файл | Назначение |
|------|------------|
| `CardSystem.ts` | Управление рукой, drag-drop |
| `CardStackingSystem.ts` | Логика стакинга |
| `ForgeSystem.ts` | Кузница (2→1 upgrade + эволюция) |
| `cards/*.ts` | Определения типов карт |
| `Tower.ts` | Применение карт к статам |
| `src/ui/ShopUI.ts` | UI магазина (рендеринг слотов, покупка, реролл) |
| `src/ui/GameHUD.ts` | HUD (жизни, золото, волна, кнопки) |

### Ключевые функции

```typescript
// CardStackingSystem.ts
mergeCardsWithStacking(cards: ICard[]): MergedCardData

// ForgeSystem.ts
canForgeCards(cards: ICard[]): boolean
forgeCards(cards: ICard[]): ICard

// CardSystem.ts
onCardDropOnTower(card: ICard, tower: Tower)
// CardSystem.ts
onCardDropOnTower(card: ICard, tower: Tower)
onCardDropOnForge(card: ICard)
```

### Idempotency (Drop Safety)

To prevent duplicate card effects (e.g., double-clicks or lag spikes causing two towers to spawn), every drop event generates a unique `actionId`.

- `CardSystem` generates `actionId`.
- `GameController` tracks `recentActionIds` (Set) and ignores duplicates.
