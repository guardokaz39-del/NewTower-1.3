import { Game } from './Game';
// import { Enemy } from './Enemy'; // Больше не нужно импортировать врага вручную

window.addEventListener('DOMContentLoaded', () => {
    
    const canvas = document.getElementById('game-canvas');
    
    if (!canvas) {
        console.error("❌ ОШИБКА: Canvas с id='game-canvas' не найден!");
        alert("Ошибка: Игра не может найти экран. Проверьте консоль.");
        return;
    }

    try {
        console.log("✅ Canvas найден. Инициализация систем...");
        const game = new Game('game-canvas');

        // Запускаем игровой цикл
        game.start();

        // Сразу запускаем первую волну!
        // Это создаст врагов правильно, используя координаты карты
        console.log("🌊 Запуск первой волны...");
        game.startWave();
        
    } catch (e) {
        console.error("Критическая ошибка при запуске:", e);
    }
});